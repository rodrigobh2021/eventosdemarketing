# Database — Connection string do PostgreSQL (Supabase ou Neon)
DATABASE_URL="postgresql://user:password@host:port/db"

# NextAuth — Autenticacao com Auth.js v5
NEXTAUTH_SECRET="gerar_com_openssl_rand_-base64_32"
NEXTAUTH_URL="http://localhost:3000"
GOOGLE_CLIENT_ID="seu_google_client_id"
GOOGLE_CLIENT_SECRET="seu_google_client_secret"

# Resend — Servico de envio de emails
RESEND_API_KEY="re_sua_api_key"
EMAIL_FROM="noreply@seudominio.com.br"

# Google Maps — API para exibir mapas na pagina do evento
GOOGLE_MAPS_API_KEY="sua_google_maps_api_key"

# Claude API — Usado pelo agente de scraping (Anthropic)
ANTHROPIC_API_KEY="sk-ant-sua_api_key"

# App — URLs e nome publico do site
NEXT_PUBLIC_SITE_URL="https://eventosdemarketing.com.br"
NEXT_PUBLIC_SITE_NAME="Eventos de Marketing"

# Protecao temporaria do site (remover apos lancamento publico)
# Defina SITE_PROTECTION_ENABLED="false" para desativar sem remover as vars
SITE_PROTECTION_ENABLED="false"
SITE_PROTECTION_USER="admin"
SITE_PROTECTION_PASSWORD="senha_segura_aqui"
